services:
  mcp-server:
    build: .
    ports:
      - "3001:3001"   # MCP Server (SSE)
      - "8545:8545"   # Anvil RPC
    volumes:
      - ./projects:/app/projects  # User projects mounted
    environment:
      - NODE_ENV=production
      - ANVIL_BLOCK_TIME=1
