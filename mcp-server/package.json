{
  "name": "mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vitest": "^2.20.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
