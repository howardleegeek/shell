{
  "name": "mcp-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist', {recursive: true}); require('fs').writeFileSync('dist/server.js', 'console.log(\\'MCP Server placeholder\\')');\""
  }
}
