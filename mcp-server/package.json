{
  "name": "shell-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Shell MCP Server providing Web3 tooling via MCP protocol.",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.0.0",
    "zod": "^3.20.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^3.0.0",
    "@types/node": "^20.0.0"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc",
    "dev": "tsx watch src/server.ts",
    "test": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=18"
  }
}
