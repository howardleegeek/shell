{
  "name": "shell-web-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/abi-manager.test.mjs"
  },
  "scripts": {
    "dev": "node server.js",
    "test": "vitest run"
  }
}
