Shell Web UI Fork

This web UI is a fork of bolt.diy deployed under the shell repository.
The UI serves as a lightweight frontend base to host bolt.diy-like tooling.

How to run:
- cd web-ui
- pnpm install
- pnpm dev

Open http://localhost:3000 to view the UI.
