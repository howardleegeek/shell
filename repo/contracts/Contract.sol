// Minimal Solidity contract used by Migration Wizard for testing
pragma solidity ^0.8.0;

contract Test {
  function transfer(address to, uint256 amount) public returns (bool) {
    // dummy implementation
    return true;
  }
}
